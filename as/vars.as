// where global variables are stored

var new_size_influence, mid_point_push, max_line_width;
_global.where.new_size_influence = (Math.floor(Math.random() * 20) / 10) - 0.5;
_global.where.mid_point_push = (Math.floor(Math.random() * 8) / 4) - 1;
_global.where.new_size_influence = 0.25;// 0.5;
_global.where.mid_point_push = 0; //0.75;
_global.where.max_line_width = (Math.random() * 50) + 50;

_global.tic_spacing = 5;
_global.left_offset = 0;
_global.top_offset = 0;

_global.where.line_numbers = 15;
